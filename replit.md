# Brainstorm Shell

## Overview
Brainstorm Shell is a minimal web application built with React, TypeScript, and Vite, designed to provide a user interface for the Brainstorm platform. It integrates real Nostr login capabilities via NIP-07 browser extensions, enabling secure challenge-response authentication with the Brainstorm backend API. The application focuses on social graph visualization, GrapeRank reputation scoring, and Web of Trust (WoT) concepts, aiming to offer users insights into their social network and influence within the Nostr ecosystem. Key capabilities include profile metadata fetching, social graph exploration, and NIP-85 service provider activation.

## User Preferences
I prefer detailed explanations and clear communication. When making changes, please ask before implementing major architectural decisions. I value an iterative development approach and clear, concise code. I also prefer the use of dark themes and modern UI/UX principles.

## System Architecture
The application is structured around a React 18 frontend with TypeScript, Vite, Tailwind CSS, and shadcn/ui for UI components. Routing is handled by `wouter`, defining distinct paths for landing, login, onboarding, dashboard, search, network, settings, and an educational "What is WoT?" page. The backend utilizes Express.js for API proxying, particularly for Nostr profile fetching and NIP-05 resolution, mitigating CORS issues.

**Key Architectural Decisions:**
- **UI/UX:** Employs a dark-theme aesthetic with glassmorphism elements, gradient texts, and CSS-only animations (avoiding `framer-motion` where possible for performance and simplicity). Features polished designs for login, dashboard, and other core pages, incorporating elements like animated graph backgrounds, custom SVG icons, and interactive data visualizations.
- **Nostr Integration:** Leverages `applesauce-core` for Nostr functionalities like NIP-07 for login and profile helpers, and `nostr-tools` for `nip19` encoding. Authentication follows a challenge-response mechanism with the Brainstorm backend using kind 22242 events.
- **Data Management:** Uses React Query for efficient data fetching, caching, and state management, particularly for user profiles, GrapeRank results, and social graph data.
- **Feature Specifications:**
    - **Authentication:** NIP-07 based challenge-response login, storing session tokens in `localStorage`.
    - **Dashboard:** Displays GrapeRank results, social graph counts (followers, following, etc.), and provides an onboarding panel.
    - **Search Page:** Lightweight lookup tool — accepts `npub`, hex pubkeys, and NIP-05 handles. On successful lookup, redirects to the Profile page. No profile display logic; purely a search form.
    - **Profile Page (`/profile/:npub`):** Dedicated profile viewer showing trust score, social reach (followers/following/mutual), risk assessment (muted_by/reported_by/muting/reporting), shared connections with logged-in user, expandable drill-down lists, and raw API data. Drill-down on expanded profiles navigates to their own `/profile/:npub`. "Back to Network" button appears when navigated from Network page via `?fromGroup=` param.
    - **Network Page:** A CRM-style social graph explorer, enabling users to view and manage their connections by group with filtering and search capabilities.
    - **What is WoT? Page:** An educational resource with interactive trust scenarios and visualizations.
    - **NIP-85 Service Provider Activation:** A guided process for activating Brainstorm as a NIP-85 service provider, involving kind 10040 event publishing.
    - **Settings Page:** Manages NIP-85 status, GrapeRank calculation triggers, and future trust perspective settings.
- **Performance Optimizations:** Implements 60-second timeouts for server proxy routes and defers profile fetching to the dashboard for quicker login experiences. Uses branded BrainLogo pulse animations for loading states.

## External Dependencies
- **Nostr Protocol:** Utilizes various Nostr relays (e.g., damus, nostr.band, nos.lol, primal, purplepag.es, nostr.info, nostr.wine, snort.social) for fetching kind:0 metadata and publishing events.
- **Brainstorm Backend API:** `https://brainstormserver.nosfabrica.com` for authentication challenges, verification, user data (`/user/self`, `/user/:pubkey`), GrapeRank results (`/graperankResult`), and NIP-05 proxying.
- **Nostr HTTP APIs:** `api.nostr.band` and `purplepag.es` serve as HTTP fallback options for profile fetching.
- **NIP-07 Browser Extension:** Required for Nostr key management and event signing.

## Recent Changes
- Network page gated behind GrapeRank calculation: nav links grayed out on all pages until `calcDone`, NetworkPage shows branded gate screen if accessed directly before completion. Trusted Followers card on Dashboard also disabled until calcDone.
- Settings "Recalculate GrapeRank" button: triggers API, invalidates cache, shows toast, redirects to Dashboard where onboarding panel reappears with polling. Button disabled with spinner when calculation already in progress.
- Onboarding condensed to 2 steps: Calculating (internal_publication_status=success) + Publishing (ta_status=success). Logout clears all React Query cache.
- Contextual onboarding language: `isRecalculation` flag detects returning users (have `last_time_calculated_graperank` or `nip85Activated`). First-time users see "Welcome" onboarding; recalculating users see "Refreshing your trust scores" with reassuring copy. Trust score badge shows "Recalculating…" vs "Awaiting calculation" accordingly.
- Onboarding error handling: `isPublishFailed` detects partial failure (calc done, ta_status failure). Retry button allows 1 attempt then shows "Go to Settings". `retryCount` resets when failure clears. Settings page shows amber warning banner when GrapeRank is in a failed state (hidden during active recalc).
- Network page performance: profile batch size increased 5→10, trust score batch size 3→8, next-page pre-fetching for profiles and trust scores so pagination feels instant.
- Network page rendering optimization: Profile card extracted into `NetworkProfileCard` memoized component (`React.memo`) with resolved data as props, preventing full-list DOM reconciliation during batch loading. Static constants (`getVerificationGuidance`, `detailMetrics`, `metricIcons`) hoisted to module scope. Decorative style objects (`connectionLineStyles`, `floatingNodeStyles`, `decorativeTextStyles`) pre-computed as static arrays. Callbacks (`handleCopyNpub`, `handleCloseDetail`) wrapped in `useCallback` for stable references.
- Search page skeleton: `animate-pulse` skeleton card renders while `isSearching && hasSearched`, preventing layout jump before results appear.
- Trust tier thresholds updated across Dashboard, Network, and Search pages to use raw influence score breakpoints: Highly Trusted ≥0.50, Trusted ≥0.20, Neutral ≥0.07, Low Trust ≥0.02, Unverified <0.02. Profiles below 0.02 or in flagged groups (muted/reported) are classified as Unverified.
- Social Graph card on Dashboard shows verified counts (influence ≥0.02 and not flagged) for both Followers and Following, with raw totals still shown in the bottom Network info panel.
- Network Health card has Followers/Following pill toggle (visible at hop 1 only). `directFollowingTierCounts` mirrors `directTierCounts` but iterates `network.following`. `followingPieData` built from following tier counts. Toggle switches pie chart data, section header/subtitle, row labels ("X of your followers" vs "X you follow"), and click-through navigation group (`followed_by` vs `following`). Hops 2+ hide the toggle and reset to followers view.
- Network page now uses React Query with shared `["/api/auth/self"]` cache key (same as Dashboard), so navigating from Dashboard preset filters loads data instantly from cache instead of re-fetching. `visiblePubkeyPage` memo dependencies fixed to include `visiblePubkeys` and `trustLoadedCount` for reactive updates as profiles/trust scores load.
- Profile page separated from Search page: `/profile/:npub` is a dedicated profile viewer with all trust/social/risk display logic. Search page simplified to a lightweight lookup form that redirects to `/profile/:npub` on success. Network page "View full profile" button now navigates to `/profile/:npub?fromGroup=...` instead of `/search?npub=...`. Drill-down in profile expanded lists navigates to `/profile/:npub` for the clicked user.
- Removed self-influence display from Dashboard Network panel (always shows 1 for logged-in user).
- ProfilePage enhanced with five computed-from-existing-data features (zero new API calls): (1) Trust Tier Badge — color-coded tier name (Highly Trusted/Trusted/Neutral/Low Trust/Unverified) replaces generic "Trust Score" label on the ring, using same thresholds as Dashboard/Network. (2) Confidence Guidance — plain-language banner below profile header explaining what the trust score means. (3) Verified Follower/Following Ratios — "X of Y verified" annotation on Followers and Following rows. (4) Audience Quality Breakdown — horizontal stacked bar between Social Reach and Risk Assessment showing follower distribution by trust tier. (5) Verified Muter/Reporter Counts — "X verified" annotations on Muted By and Reported By rows; risk level calculation now weights verified reporter/muter signals more heavily; risk advisory text includes verified counts.
- `fetchProfile()` in nostr.ts now has a 10-second timeout via `Promise.race` to prevent indefinite hangs when relays don't respond. ProfilePage data loading decoupled: API call controls `isLoading`, Nostr profile fills in asynchronously.
- ProfilePage expandable sections (Followers, Following, Muted By, etc.) now have sort and filter controls. Sort options: Trust ↓ (default), Trust ↑, A-Z, Z-A. Filter options: All, Verified (≥0.02), Highly Trusted, Trusted, Neutral, Low Trust, Unverified. All operations are pure client-side on cached data — zero API calls, zero performance impact. Pagination works on the filtered/sorted list. Sort/filter state resets when a section is collapsed.
- ProfilePage expanded sections now include inline search: type a name, NIP-05, or npub to instantly narrow the list. Search input with clear button in the toolbar, works alongside sort/filter. Search state resets on collapse.
- ProfilePage section headers (Followers, Following, Muted By, Reported By, Muting, Reporting) now show tier breakdown badges — small colored dots with counts for each trust tier (Highly Trusted, Trusted, Neutral, Low, Unverified) visible at a glance before expanding. Computed from embedded influence data via `toInfluenceMap()`, zero API calls.
- ProfilePage design polish pass to match Dashboard/Network branded aesthetics: (1) Confidence banner redesigned with gradient background, shield icons (ShieldCheck/Shield/ShieldAlert/ShieldX), font-mono trust percentage badge, two-line layout, and backdrop-blur. (2) Section headers (Social Reach, Risk Assessment) upgraded with gradient treatment (`bg-gradient-to-r from-indigo-50/60 via-slate-50/40 to-white/60`), softer borders (`border-slate-200/80`), and `shadow-sm`. (3) Expanded list toolbar gets glassmorphic styling (`backdrop-blur-sm`, subtle indigo gradient). (4) "Show more" button uses brand colors (`bg-[#3730a3]`) with shadow hover effects; "Clear filter" uses brand color. (5) Main profile card upgraded with brand glow shadow and `rounded-2xl`. (6) Expandable rows use `hover:bg-indigo-50/30` with `transition-all duration-200` for smoother interactions.
- ProfilePage layout reorganization: Audience Quality breakdown moved from standalone card into the Social Reach section as an inline sub-section after Influence row. Reduces DOM, improves scan speed — followers/following/mutual/influence/audience quality now form one cohesive card. Page section order: Header → About → Confidence Banner → Shared Connections → Social Reach (with Audience Quality) → Risk Assessment → Risk Advisory → Raw Data.
- NIP-85 10040 event now includes `30382:followers` tag alongside `30382:rank` (from vitorpamplona PR merge).
- Settings page "Update NIP-85 Event" button added to Service Provider card (Active state). Re-signs and re-publishes the 10040 event with current tags. States: idle → signing → publishing → success/error. Uses same `signNip85`/`publishToRelays` flow as ActivateBrainstormModal. Mobile responsive (full-width on small screens).